# ==============================================================================
# Unified Requirements for Octo + LIBERO Evaluation
#
# This file is carefully ordered to resolve dependency conflicts between
# the modern JAX ecosystem and older TensorFlow/robotics libraries.
#
# INSTRUCTIONS:
# 1. Create a new conda environment: `conda create -n octo-eval python=3.9 -y`
# 2. Activate it: `conda activate octo-eval`
# 3. Run: `pip install -r requirements-unified.txt`
# ==============================================================================

# -- Use --extra-index-url to ADD the PyTorch index, not replace the default --
--extra-index-url https://download.pytorch.org/whl/cu118

# ==============================================================================
# BLOCK 1: THE CORE CONFLICT RESOLUTION
# We pin the exact versions of TensorFlow and its critical dependency `ml-dtypes`
# FIRST. This prevents the JAX installation from breaking TensorFlow.
# ==============================================================================
tensorflow==2.15.0
ml-dtypes==0.2.0
numpy==1.24.1

# ==============================================================================
# BLOCK 2: THE JAX ECOSYSTEM (PINNED TO COMPATIBLE VERSIONS)
# These specific older versions are known to work with the `ml-dtypes` version
# required by TensorFlow 2.15.
# ==============================================================================
jax==0.4.20
jaxlib==0.4.20
flax==0.7.5
chex==0.1.85
optax==0.1.5
distrax==0.1.4

# ==============================================================================
# BLOCK 3: PYTORCH
# ==============================================================================
torch==2.3.1
torchvision==0.18.1

# ==============================================================================
# BLOCK 4: LIBERO & ROBOTICS STACK
# These are the foundational libraries that set many of the stricter,
# older version constraints.
# ==============================================================================
gym==0.25.2
opencv-python==4.6.0.66
robomimic==0.2.0
robosuite==1.4.0
bddl==1.0.1

# ==============================================================================
# BLOCK 5: OTHER UTILITIES & OCTO DEPENDENCIES
# ==============================================================================
future==0.18.2
wandb==0.13.1
transformers==4.21.1
einops==0.4.1
matplotlib==3.5.3
cloudpickle==2.1.0
hydra-core==1.2.0
easydict==1.9
thop==0.1.1-2209072238
ml_collections>=0.1.0
tqdm>=4.60.0
absl-py>=0.12.0
scipy==1.10.1
imageio>=2.31.1
moviepy>=1.0.3
tensorflow_hub>=0.14.0
tensorflow_datasets==4.9.2
tensorflow_graphics==2021.12.3
tensorflow_probability==0.23.0
plotly>=5.16.1

# ==============================================================================
# BLOCK 6: GIT DEPENDENCIES (INSTALLED LAST)
# dlimp depends on tensorflow, but since we've already installed the correct
# version in Block 1, this should resolve cleanly.
# ==============================================================================
dlimp @ git+https://github.com/kvablack/dlimp@5edaa4691567873d495633f2708982b42edf1972#egg=dlimp